# comb2 稳定包使用手册

本文面向 coding agent 和人类使用者，目标是在不阅读源码的情况下，知道如何用稳定包创建 research 项目、接入模型、训练生成 alpha、回测、评估、做常见扩展和排错。

本文只覆盖稳定使用接口，不覆盖包内部实现和 vendor 目录可改点。

## 1. 这个包做什么

`comb2` 稳定包把 research 模型接到统一的数据、训练、alpha 生成、回测和本地评估流程上。

典型工作流：

```text
创建项目目录
  -> 编辑 config.xml
  -> 实现 ResearchModel
  -> runCombo config.xml
  -> runEval config.xml
  -> 按评估结果调整模型、数据或回测配置
```

稳定命令入口：

| 命令 | 用途 |
|---|---|
| `combo-hello-world` | 在当前目录创建可编辑的 `Model.py`、`config.xml`、`config.human` starter。 |
| `runCombo config.xml` | 训练 research 模型，生成 alpha，并执行回测。 |
| `runEval config.xml` | 对一个 config 的输出做总体评估。 |
| `runEval --sim ...` | 对 signal 或 daily IC 做 IC 类评估。 |
| `runEval --pnl ...` | 对 signal 或 daily pnl 做 PnL 类评估。 |
| `runEval --corr ...` | 对两个 parquet/csv 矩阵做日频截面相关性评估。 |
| `runEval --exposure ...` | 对 signal 做 Barra 风格暴露评估。 |
| `runEval --va ...` | 对 base/new PnL 或 signal 做 value-add 评估。 |
| `comb-ablation-zero config.xml` | 可选：逐特征置零消融。 |
| `comb-pos-corr pos_a.parquet pos_b.parquet` | 可选：两个 position parquet 的平均日频持仓相关。 |

TODO: Optuna 调参流程需要稳定命令接口化后再纳入本手册。当前不要把 Optuna 作为稳定包主流程依赖。

## 2. 推荐目录结构

一个 research 项目目录建议长这样：

```text
my-research/
  config.xml
  Model.py
  output/
  checkpoints/
```

可选扩展文件：

```text
my-research/
  loader.py
  dataset.py
  strategy.py
  data_packs/
    factors.xml
```

各文件职责：

| 文件 | 是否必须 | 稳定约定 |
|---|---|---|
| `config.xml` | 必须 | 声明数据、日期、模型路径、训练参数、回测参数、输出路径。 |
| `Model.py` | 必须 | 定义 `ResearchModel` 类。 |
| `loader.py` | 可选 | 定义 `ResearchLoader`，自定义 feature/label 预处理或生成方式。 |
| `dataset.py` | 可选 | 定义 `ResearchDataset`，自定义训练样本和股票池。 |
| `strategy.py` | 可选 | 定义 `AlphaStrategy`，自定义 alpha 到持仓的回测逻辑。 |
| `data_packs/*.xml` | 可选 | 复用一组 `<data><item>` 数据声明。 |

`config.xml` 中的相对路径都相对 `config.xml` 所在目录解析，因此命令可以从任意工作目录执行。

## 3. 从零开始

创建 starter：

```bash
mkdir my-research
cd my-research
combo-hello-world
```

如果确认覆盖当前目录已有 starter 文件：

```bash
combo-hello-world --force
```

然后按顺序修改：

1. `config.xml` 的 `<constants>`：填真实数据根目录、因子根目录、输出目录、checkpoint 目录。
2. `config.xml` 的 `<combo><data>`：填要使用的 factor 和 label。
3. `config.xml` 的 `<strategy>`：填回测日期范围，必要时接入自定义 strategy。
4. `Model.py`：实现自己的 `ResearchModel`。
5. 跑训练和回测：

```bash
runCombo config.xml
```

6. 跑总体评估：

```bash
runEval config.xml
```

## 4. 完整 config.xml 模板

这个模板包含稳定可设置参数的默认形态。实际项目通常只需要改路径、日期、数据列表、模型参数和少数运行参数。

```xml
<config>
  <constants
    cache_path="data/Cache"
    output_root="output"
    checkpoint_root=""
  />

  <strategy
    start_ds="20160111"
    end_ds="20200101"
  />

  <combo>
    <paths
      base_dir="."
      output_dir="output"
      model_path="Model.py"
      combo_base_path=""
      research_loader_path=""
      research_dataset_path=""
      checkpoint_root=""
    />

    <output
      alpha_history_path="output/alpha_history.pt"
      log_path="output/train.log"
      enable_alpha_analysis="true"
    />

    <runtime
      snaptime="mlp_minimal"
      snap_ti=""
      seed="42"
      livetrading="false"
      trainDelay="0"
      retDays="1"
      tsDays="8"
      load_chunk_days=""
      processed_feature_cache="false"
      torch_threads="64"
      torch_interop_threads="1"
      model_smooth_rate="0.7"
      model_keep_num="2"
      select_days="100"
      max_train_days="2000"
      verbose="false"
    />

    <model
      device="cpu"
      dropout="0.5"
      lr="0.000002"
      epochs="15"
      batch_size="3"
      weight_decay="0.000001"
      scheduler_step_size="10"
      scheduler_gamma="0.5"
      grad_clip="10.0"
      early_stopping_patience="5"
    />

    <data
      dtype="float16"
      compression="none"
      data_start_ds="20160101"
      ashare_data_path=""
      valid_path=""
      filtered_path=""
      base_universe_path=""
    >
      <item name="factor.example_1" module="builtin.factorsim" path="example_factor_1" role="factor" display_name="example_factor_1" />
      <item name="factor.example_2" module="builtin.factorsim" path="example_factor_2" role="factor" display_name="example_factor_2">
        <op name="cs_zscore" />
        <op name="truncate" min="-4" max="4" />
        <op name="nan_to_num" value="0" />
      </item>
      <item name="label.default" module="builtin.label" path="vwap30_label1d" role="label" />
    </data>

    <defaults
      selection_module=""
    />
  </combo>

  <backtest
    output_path="output/backtest"
    daily_metrics_file="daily_pnl.csv"
    cash="10000000.0"
    fee_rate="0.0015"
    reserve_cash="0.95"
    verbose="false"
    universe="base"
    execution_price="vwap30"
    drawdown_stop="0.0"
    cooldown_days="0"
  />

  <monitor
    enabled="false"
    output_path=""
    format="csv"
    print_summary="true"
    collect_gpu="true"
    sync_cuda="false"
    verbose="false"
  />
</config>
```

## 5. XML 通用规则

根节点必须是 `<config>`。

除 `<combo><model ... />` 外，其他稳定节点只接受已定义属性。`<combo><model>` 是开放字段，允许 research 模型自定义参数。

常用类型写法：

| 类型 | 写法 |
|---|---|
| bool true | `1`, `true`, `yes`, `y`, `on` |
| bool false | `0`, `false`, `no`, `n`, `off` |
| int/float | 按默认字段类型转换，例如 `max_train_days="2000"`。 |
| dtype | `float16`, `float32`, `float64`, `bfloat16` |
| None | 对允许为空的字段写空字符串，例如 `checkpoint_root=""`。 |

路径规则：

- 绝对路径保持不变。
- 相对路径相对 `config.xml` 或 data-pack 文件所在目录解析。
- `builtin.factorsim` 不再使用隐式 factor root；普通 factor pool 请写绝对路径，或写相对当前 XML/data-pack 的路径。
- `builtin.label` 的非绝对 `path` 会解析到 `{AshareCache}/1d_DailyLabel/DailyLabel.{path}`，例如 `path="vwap30_label1d"` 会解析到 `DailyLabel.vwap30_label1d`。
- `constants.cache_path` 会派生出 `AshareCache`，供 label、mask、Barra、回测读取使用。

## 6. constants

```xml
<constants
  cache_path="data/Cache"
  output_root="output"
  checkpoint_root=""
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `cache_path` | `data/Cache` | 行情、mask、label 等缓存根目录。运行时会使用其下的 `AshareCache`。 |
| `output_root` | `output` | alpha、日志、回测、评估报告的默认输出根目录。 |
| `checkpoint_root` | 空 | checkpoint 根目录。空值表示不通过 constants 统一派生 checkpoint。 |

## 7. strategy: 回测持仓逻辑接入

```xml
<strategy start_ds="20200101" end_ds="20240628" />
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `start_ds` | `20160111` | 回测开始日期，格式 `YYYYMMDD`。 |
| `end_ds` | `20200101` | 回测结束日期。 |
| `path` | 空 | 可选自定义 strategy 文件。省略时使用包内默认 `AlphaStrategy`。 |

默认 strategy 行为：

- 每天读取模型生成的 alpha 截面。
- 对有效 alpha 减去当日截面中位数。
- 只持有调整后为正的股票。
- 按调整后正值归一化生成 long-only 权重。

自定义 strategy：

```xml
<strategy
  start_ds="20200101"
  end_ds="20240628"
  path="strategy.py"
/>
```

`strategy.py` 必须定义 `AlphaStrategy`：

```python
from comb2_pcmaster.strategy import StrategyBase


class AlphaStrategy(StrategyBase):
    def generate_positions(self, signals, last_hold):
        weights = signals.dropna()
        weights = weights[weights > weights.median()]
        if weights.empty:
            return weights
        return weights / weights.sum()
```

接口约定：

| 名称 | 约定 |
|---|---|
| `signals` | 当日 alpha 截面，通常是 index 为股票代码、value 为 alpha 的 pandas Series。 |
| `last_hold` | 上一期持仓，可用于控制换手；不用时可以忽略。 |
| 返回值 | index 为股票代码、value 为目标权重的 pandas Series。 |

## 8. combo.paths

```xml
<paths
  base_dir="."
  output_dir="output"
  model_path="Model.py"
  combo_base_path=""
  research_loader_path=""
  research_dataset_path=""
  checkpoint_root=""
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `base_dir` | `.` | 稳定包场景通常不需要修改。 |
| `output_dir` | `output` | 训练和 alpha 输出目录，通常与 `constants.output_root` 保持一致。 |
| `model_path` | `Model.py` | research 模型文件，必须定义 `ResearchModel`。 |
| `combo_base_path` | 空 | 可选运行时扩展文件，必须定义继承 `comb2.ComboBase` 的 `ComboBase`。适合覆写 `isTrainDay()`、`needTrain()` 等训练调度逻辑。 |
| `research_loader_path` | 空 | 可选 loader 扩展文件，必须定义 `ResearchLoader`。 |
| `research_dataset_path` | 空 | 可选 dataset 扩展文件，必须定义 `ResearchDataset`。 |
| `checkpoint_root` | 空 | checkpoint 目录；空值表示不由该字段启用。 |

### 8.1 自定义 ComboBase 训练调度

如果只想改训练日判定，不需要改 vendor 目录。可以在项目目录新增一个 `combo_base.py`，然后在 XML 中接入：

```xml
<paths
  model_path="Model.py"
  combo_base_path="combo_base.py"
/>
```

`combo_base.py` 必须定义一个继承 `comb2.ComboBase` 的 `ComboBase` 类。最常见场景是覆写 `isTrainDay(ds)`：

```python
from __future__ import annotations

from comb2 import ComboBase as BaseComboBase


class ComboBase(BaseComboBase):
    def isTrainDay(self, ds: int) -> bool:
        didx = self.loader.date2didx(ds)
        return didx % 5 == 0
```

`isTrainDay(ds)` 的输入和语义：

| 项目 | 说明 |
|---|---|
| `ds` 类型 | `YYYYMMDD` 格式的整数，例如 `20240628`。 |
| `ds` 含义 | 这里传入的是训练目标日 `target_ds`，不是当前回测循环日。若设置了 `trainDelay`，框架会先做日期偏移，再把偏移后的 `target_ds` 传进来。 |
| 返回 `True` | 表示这个 `target_ds` 按你的规则属于训练日。 |
| 返回 `False` | 表示这个 `target_ds` 按你的规则不属于训练日。 |
| 最终是否训练 | 还会再经过 `needTrain(ds)` 的 checkpoint 判定；`isTrainDay()` 只负责训练日历规则本身。 |

写 `isTrainDay()` 时最常用的辅助能力：

| 可用对象 / 方法 | 用途 |
|---|---|
| `self.loader.date2didx(ds)` | 把 `YYYYMMDD` 转成交易日序号。适合按“每 N 个交易日”写规则。 |
| `self.loader.didx2date(didx)` | 把交易日序号转回 `YYYYMMDD`。适合向前向后看相邻交易日。 |
| `self.loader.mask.date` | 全部交易日数组。需要遍历或判断边界时可直接使用。 |

建议优先按“交易日序号”写逻辑，而不是按自然日差值写逻辑。A 股节假日和停牌日会让自然日规则变得不稳定。

接口约定：

| 名称 | 约定 |
|---|---|
| 类名 | 必须叫 `ComboBase`。 |
| 继承关系 | 必须继承 `comb2.ComboBase`。 |
| `isTrainDay(ds)` | 只控制某个 `target_ds` 是否是训练日。 |
| `needTrain(ds)` | 如果还要改 checkpoint 命中后的重训判定，可以进一步覆写它。 |

建议：

- 只改训练日规则时，优先覆写 `isTrainDay(ds)`。
- 只有在你明确需要改变 checkpoint 复用或过期策略时，再覆写 `needTrain(ds)`。
- 保持方法签名不变，不要改 `ComboBase.__init__` 的输入协议。

常见示例：

1. 每 5 个交易日训练一次

```python
from comb2 import ComboBase as BaseComboBase


class ComboBase(BaseComboBase):
    def isTrainDay(self, ds: int) -> bool:
        return self.loader.date2didx(ds) % 5 == 0
```

2. 每周最后一个交易日训练一次

```python
import datetime

from comb2 import ComboBase as BaseComboBase


class ComboBase(BaseComboBase):
    def isTrainDay(self, ds: int) -> bool:
        didx = self.loader.date2didx(ds)
        if didx >= len(self.loader.mask.date) - 1:
            return True
        today = datetime.datetime.strptime(str(self.loader.didx2date(didx)), "%Y%m%d")
        next_day = datetime.datetime.strptime(str(self.loader.didx2date(didx + 1)), "%Y%m%d")
        return (next_day - today).days > 1
```

3. 每月最后一个交易日训练一次

```python
from comb2 import ComboBase as BaseComboBase


class ComboBase(BaseComboBase):
    def isTrainDay(self, ds: int) -> bool:
        didx = self.loader.date2didx(ds)
        if didx >= len(self.loader.mask.date) - 1:
            return True
        this_month = str(ds)[4:6]
        next_month = str(self.loader.didx2date(didx + 1))[4:6]
        return this_month != next_month
```

4. 只在白名单日期训练

```python
from comb2 import ComboBase as BaseComboBase


TRAIN_DATES = {
    20240131,
    20240229,
    20240329,
}


class ComboBase(BaseComboBase):
    def isTrainDay(self, ds: int) -> bool:
        return int(ds) in TRAIN_DATES
```

## 9. ResearchModel 接口

`model_path` 指向的文件必须定义：

```python
class ResearchModel:
    def __init__(self, config: dict):
        ...

    def fit(self, dataset):
        ...

    def predict(self, x_window):
        ...

    def save(self, path_or_buffer):
        ...

    def load(self, path_or_buffer):
        ...
```

最小可运行形态：

```python
from __future__ import annotations

import torch


class ResearchModel:
    def __init__(self, config: dict):
        self.config = config
        self.dtype = config.get("dtype", torch.float16)
        self.ts_days = int(config.get("tsDays", 8))
        self.num_features = int(config.get("num_features", 1))

    def fit(self, dataset):
        return self

    def predict(self, x_window: torch.Tensor) -> torch.Tensor:
        return torch.zeros(x_window.shape[1], dtype=self.dtype)

    def save(self, path_or_buffer):
        torch.save({"config": self.config}, path_or_buffer)

    def load(self, path_or_buffer):
        payload = torch.load(path_or_buffer, map_location="cpu")
        self.config = payload.get("config", {})
        return self
```

稳定约定：

| 接口 | 约定 |
|---|---|
| `__init__(config)` | `config` 来自 `<combo><model>`，并额外注入 `dtype`、`tsDays`、`num_features`。 |
| `fit(dataset)` | 训练模型。默认 dataset 样本形态为 `(idx, x, y, w)`。 |
| `predict(x_window)` | 输入通常为 `[tsDays, n_valid_instruments, num_features]` tensor。 |
| `predict` 返回 | `[n_valid_instruments]` tensor，表示预测 alpha。 |
| `save/load` | 用于 checkpoint 保存和加载，需能接受文件路径或 file-like buffer。 |

稳定 `predict(x_window)` 只接收 feature 窗口，不额外传入日期、loader 或 dataset。若预测阶段需要日期相关辅助数据，应在 `fit()` 阶段写入模型状态并通过 `save/load` 保留，或在项目自定义流程中显式扩展接口。

`<combo><model>` 是开放字段。字段是否生效，取决于 `ResearchModel(config)` 是否读取它。

## 10. combo.output

```xml
<output
  alpha_history_path="output/alpha_history.pt"
  log_path="output/train.log"
  enable_alpha_analysis="true"
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `alpha_history_path` | `output/alpha_history.pt` | 保存按日期组织的 alpha tensor 历史。 |
| `log_path` | `output/train.log` | 训练日志路径。 |
| `enable_alpha_analysis` | `true` | 是否在 `runCombo` 结束后导出 `alpha.parquet` 和 `daily_ic`。快速调试可设为 `false`，正式评估需要 `alpha.parquet`。 |

## 11. combo.runtime

```xml
<runtime
  snaptime="mlp_minimal"
  snap_ti=""
  livetrading="false"
  deterministic="false"
  trainDelay="0"
  retDays="1"
  tsDays="8"
  load_chunk_days=""
  processed_feature_cache="false"
  torch_threads="64"
  torch_interop_threads="1"
  model_smooth_rate="0.7"
  model_keep_num="2"
  select_days="100"
  max_train_days="2000"
  verbose="false"
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `snaptime` | `mlp_minimal` | 训练快照名，checkpoint 子目录会使用它。 |
| `snap_ti` | 空 | 默认数据时点；空值时使用该数据频率最后一个可用时点。对 3D `builtin.factorsim` 数据会影响日内切片位置。 |
| `seed` | 空 | 运行级随机种子；会用于 `random`、`numpy`、`torch`，并在有 CUDA 时同步设置 CUDA seed。 |
| `deterministic` | `false` | 是否开启 PyTorch deterministic 模式。开启后通常更可复现，但可能变慢，且部分算子可能报 deterministic 限制。 |
| `livetrading` | `false` | `false` 为历史训练/回测流程。 |
| `trainDelay` | `0` | 训练目标相对当前日期的延迟。 |
| `retDays` | `1` | 训练 label 的收益 horizon。 |
| `tsDays` | `8` | 模型输入时间窗口长度。 |
| `load_chunk_days` | 空 | 训练数据预取天数；空值时使用默认行为。 |
| `processed_feature_cache` | `false` | 是否缓存处理后的 feature。可提速，但增加内存。 |
| `torch_threads` | `64` | PyTorch intra-op 线程数。 |
| `torch_interop_threads` | `1` | PyTorch inter-op 线程数。 |
| `model_smooth_rate` | `0.7` | 新旧模型预测平滑比例；`1.0` 表示只用新模型。 |
| `model_keep_num` | `2` | 保留 checkpoint 个数；`0` 表示不保存，负数表示不清理。 |
| `select_days` | `100` | 训练样本选择窗口参数。普通用户通常保持默认。 |
| `max_train_days` | `2000` | 每次训练最多加载的训练日数。 |
| `verbose` | `false` | 是否打印更详细进度。 |

补充说明：

- `snap_ti` 是默认时点上下文。若调用方在运行时显式传入 `ti`，运行时 `ti` 会覆盖默认 `snap_ti`。
- `snap_ti` / 运行时 `ti` 目前主要影响 3D `builtin.factorsim` 数据在某个交易日内截取到哪个时点，以及 `nbar` 选窗后的可见 `bar` 轴范围。

常用调优：

| 目标 | 建议 |
|---|---|
| 降低内存 | 降低 `max_train_days`，关闭 `processed_feature_cache`，或使用 `compression="fp4"`。 |
| 提升重复训练读取速度 | 开启 `processed_feature_cache="true"`。 |
| 禁用 checkpoint | `model_keep_num="0"`，并让 checkpoint 目录为空。 |
| 快速 smoke run | 缩短 `strategy` 日期范围，降低 `epochs`、`max_train_days`。 |

## 12. combo.model

`combo.model` 是 research 模型参数区。starter 默认使用：

```xml
<model
  device="cpu"
  dropout="0.5"
  lr="0.000002"
  epochs="15"
  batch_size="3"
  weight_decay="0.000001"
  scheduler_step_size="10"
  scheduler_gamma="0.5"
  grad_clip="10.0"
  early_stopping_patience="5"
/>
```

说明：

- 这些字段只是 starter `ResearchModel` 的默认约定。
- 可以添加任意自定义字段，例如 `hidden_size="256"`、`n_layers="3"`。
- 数值会尽量解析为 bool/int/float，否则保留字符串。
- 稳定包会额外注入 `dtype`、`tsDays`、`num_features`。
- 兼容别名：`hiddenSize -> hidden_size`、`fcSize -> fc_size`、`batchSize -> batch_size`。

## 13. combo.data

```xml
<data
  dtype="float16"
  compression="none"
  data_start_ds="20160101"
  data_offset="1024"
  ashare_data_path=""
  valid_path=""
  filtered_path=""
  base_universe_path=""
>
  <item name="factor.example_1" module="builtin.factorsim" path="example_factor_1" role="factor" />
  <item name="label.default" module="builtin.label" path="vwap30_label1d" role="label" />
</data>
```

### 13.1 data 属性

| 参数 | 默认值 | 说明 |
|---|---|---|
| `dtype` | `float16` | loader 输出 dtype。 |
| `compression` | `none` | 内部特征缓存压缩：`none`、`fp4`、`fp8`。 |
| `data_start_ds` | `20160101` | 数据业务起点。 |
| `data_offset` | `1024` | 交易日轴起始偏移；会在构建 universe 时跳过更早的 mask/date 数据。通常保持默认，只有明确知道需要更早历史或更小 universe 起点时再改。 |
| `ashare_data_path` | 空 | AshareCache 根目录；空值时从 `constants.cache_path` 派生。 |
| `valid_path` | 空 | valid mask 路径；空值时从 AshareCache 派生。 |
| `filtered_path` | 空 | filtered mask 路径；空值时从 AshareCache 派生。 |
| `base_universe_path` | 空 | base universe mask 路径；空值时从 AshareCache 派生。 |

压缩说明：

| 值 | 说明 |
|---|---|
| `none` | 不压缩，最保守。 |
| `fp4` | 4-bit 近似量化，主要降低 CPU 内存。 |
| `fp8` | 使用 PyTorch float8，依赖当前 PyTorch 环境支持。 |

压缩只作用于内部特征缓存，不改变 label、mask、原始因子文件、模型参数和模型输入输出语义。

### 13.2 item

```xml
<item name="factor.example" module="builtin.factorsim" path="example_factor" role="factor" display_name="example_factor" />
<item name="label.default" module="builtin.label" path="vwap30_label1d" role="label" />
```

3D `builtin.factorsim` 示例：

```xml
<item
  name="factor.grid_close_mean_5"
  module="builtin.factorsim"
  path="1m_Grid1mBar/Grid1mBar.close"
  role="factor"
  nbar="5"
>
  <op name="mean" axis="bar" />
</item>
```

| 属性 | 说明 |
|---|---|
| `name` | 数据注册名，必须唯一。 |
| `module` | 数据读取方式。稳定内置值见下表。 |
| `path` | 数据路径或内置 module 参数。 |
| `role` | `factor` 进入模型 feature；`label` 作为训练 label；`aux` 只供 op 或自定义 loader 使用。 |
| 其他属性 | 会进入 item 参数，例如 `display_name`。 |

内置 module：

| module | 用途 |
|---|---|
| `builtin.factorsim` | 读取 factor 或 AshareCache 数据；绝对路径直读，相对路径相对当前 XML/data-pack 文件。 |
| `builtin.label` | 从 AshareCache label 路径读取 label。 |
| `builtin.alpha_parquet` | 读取本地 alpha parquet 矩阵。 |
| `builtin.barra_style` | 读取 Barra 风格因子。 |

当前只支持一个训练 label。多个 factor 会按 item 顺序组成 feature。

`builtin.factorsim` 补充约定：

- 2D 数据源：直接按日读取，不接受 `nbar`、`freq` 这些参数。
- 3D 数据源：可以声明 `nbar`，也可以只声明 `freq`。`nbar` 只负责在后续 `<op>` 执行前，把 `bar` 轴裁成最后 `nbar` 个位置；不负责聚合。
- 如果 3D 数据源没有显式写 `nbar`，系统会根据 `freq` 给出该频率的默认全量 `bar` 数。
- 3D 数据的可见时点仍由运行时 `ti` 决定；若未显式传入，则使用 `snap_ti`，再否则使用默认最后可用时点。
- 所有聚合都通过 `<op>` 表达，例如 `mean(axis="bar")`、`last(axis="bar")`。

实测时间轴说明：

- 当前本机 `AshareCache/1m_Grid1mBar/Grid1mBar.close` 按天实际可读出 `239` 个时点：
  上午 `09:30:00` 到 `11:30:00` 共 `121` 个，下午 `13:01:00` 到 `15:00:00` 共 `118` 个。
- 当前本机 `AshareCache/5m_Intv5mBar/Intv5mBar.close` 按天实际可读出 `49` 个时点，不是 `48` 个：
  上午从 `09:30:00` 到 `11:30:00` 共 `25` 个，下午从 `13:05:00` 到 `14:55:00` 共 `23` 个，外加收盘时点 `15:00:00`。
- 也就是说，5 分钟序列在这套数据里包含最后一个单独的 `15:00:00` 时点。

常用下标示例：

- `1m`：`0 -> 093000`，`120 -> 113000`，`121 -> 130100`，`238 -> 150000`
- `5m`：`0 -> 093000`，`24 -> 113000`，`25 -> 130500`，`48 -> 150000`

### 13.3 import

导入内置 Barra 风格因子：

```xml
<import preset="barra" />
```

导入 data-pack：

```xml
<import path="data_packs/factors.xml" />
```

只导入某类 role：

```xml
<import path="data_packs/factors.xml" role="factor" />
```

data-pack 文件只写数据声明，不写完整任务配置：

```xml
<data-pack>
  <item name="factor.turn20" module="builtin.factorsim" path="turn20" role="factor" />
  <item name="factor.size" module="builtin.factorsim" path="size" role="aux" />
</data-pack>
```

### 13.4 op

`<op>` 是 item 级预处理，写在对应 `<item>` 下：

```xml
<item name="factor.example" module="builtin.factorsim" path="example_factor" role="factor">
  <op name="rolling_mean" window="5" />
  <op name="neut(barra.size, barra.btop)" />
  <op name="neut(['size', 'beta', 'sizenl'], 0.5)" />
  <op name="cs_zscore" />
  <op name="truncate" min="-4" max="4" />
  <op name="nan_to_num" value="0" />
</item>
```

稳定 op：

| op | 说明 |
|---|---|
| `cs_zscore` / `zscore` | 截面 z-score。 |
| `rank` | 排名，可选 `axis`、`pct`。 |
| `last` | 沿指定轴取最后一个位置；必须显式给 `axis`。 |
| `mean` | 沿指定轴求均值；必须显式给 `axis`。 |
| `std` | 沿指定轴求标准差；必须显式给 `axis`。 |
| `sum` | 沿指定轴求和；必须显式给 `axis`。 |
| `max` | 沿指定轴取最大值；必须显式给 `axis`。 |
| `min` | 沿指定轴取最小值；必须显式给 `axis`。 |
| `truncate` | 截断到 `min`/`max`。 |
| `nan_to_num` / `fillna` | 缺失和无穷替换为指定值。 |
| `winsorize_by_quantile` | 分位数 winsorize，参数 `low`、`high`，可选 `axis`。 |
| `normalize_by_max_abs` | 按最大绝对值归一化，可选 `axis`。 |
| `rolling_mean` | 滚动均值，参数 `window`，默认作用在 `date` 轴；可选 `axis`。 |
| `rolling_std` | 滚动标准差，参数 `window`，默认作用在 `date` 轴；可选 `axis`。 |
| `neut(...)` | 对一个或多个已注册数据做中性化；依赖可逐个写，也可写成列表；末尾数值为扣除比例，默认 `1.0`，也可写成与依赖等长的比例列表。 |

其中轴语义统一用命名轴：

- `axis="date"`
- `axis="bar"`
- `axis="code"`

对于 3D cube item，系统会先应用 `nbar` 选窗，再按 `<op>` 顺序执行。最终 pipeline 必须收敛成 `[date, code]` 才能进入 `processed_cache`。

如果只需要改 item 级处理，优先用 `<op>`。如果要改完整 feature/label 生成逻辑，再接入 `ResearchLoader`。

## 14. ResearchLoader 和 ResearchDataset

可选接入：

```xml
<paths
  model_path="Model.py"
  research_loader_path="loader.py"
  research_dataset_path="dataset.py"
/>
```

### 14.1 ResearchLoader

`loader.py` 必须定义 `ResearchLoader`。常见用途：

| 目标 | 推荐覆盖 |
|---|---|
| 改 feature 预处理 | `preprocess_feature(feature, ds)` |
| 改 label 预处理 | `preprocess_label(label_values, valid_mask, ds, ret_days)` |
| 改完整 feature 生成方式 | `gen_feature(ds)` |
| 改完整 label 生成方式 | `gen_label(ds, ret_days)` |
| 改预测窗口处理 | `process_feature_window(feature_window)` |

最小示例：

```python
from __future__ import annotations

import torch

from comb2 import ComboDataLoader


class ResearchLoader(ComboDataLoader):
    def preprocess_feature(self, feature, ds: int):
        mean = feature.nanmean(dim=0, keepdim=True)
        std = torch.sqrt(((feature - mean) ** 2).nanmean(dim=0, keepdim=True)).clamp_min(1e-4)
        feature = (feature - mean) / std
        feature = feature.clamp(-4.0, 4.0)
        return torch.nan_to_num(feature, nan=0.0, posinf=0.0, neginf=0.0).to(self.dtype)
```

返回约定：

| 接口 | 返回 |
|---|---|
| `gen_feature(ds)` | `[N, F]` tensor，`N` 为股票数。 |
| `gen_label(ds, ret_days)` | `(y, w)`，二者 shape 都是 `[N]`。 |
| `process_feature_window(feature_window)` | 默认应保持或返回可用于预测的窗口结构。 |

读取 `<combo><data>` 中 `role="aux"` 的数据时，可在 `ResearchLoader` 中使用 `self.registry`：

```python
self.registry._ensure_range(("aux.example",), ds, ds)
idx = self.universe.date2idx(ds)
values = self.registry.get_data("aux.example")[idx]
```

`get_data(name)` 返回按 universe 对齐的完整矩阵；调用前需先确保目标日期范围已加载。

### 14.2 ResearchDataset

`dataset.py` 必须定义 `ResearchDataset`。常见用途：

| 目标 | 推荐覆盖 |
|---|---|
| 改训练股票池 | `_build_validinsts()` |
| 改训练样本返回结构 | `__getitem__(idx)` |

最小示例：

```python
from __future__ import annotations

from comb2 import ComboTrainDataset


class ResearchDataset(ComboTrainDataset):
    def _build_validinsts(self):
        return super()._build_validinsts()

    def __getitem__(self, idx: int):
        return super().__getitem__(idx)
```

如果修改 `__getitem__` 的返回结构，必须同步修改 `ResearchModel.fit()`。

## 15. 外部 data module

如果内置 module 不够，可以让 item 指向自定义读取函数：

```xml
<item
  name="factor.my_custom"
  module="my_data:load_data"
  path="my_factor.parquet"
  role="factor"
/>
```

`my_data.py`：

```python
from pathlib import Path

import pandas as pd
import torch


def load_data(item, registry, start_ds: int, end_ds: int):
    path = Path(registry.config_path or ".") / item["path"]
    frame = pd.read_parquet(path)
    dates = [ds for ds in registry.universe.dates if start_ds <= int(ds) <= end_ds]
    out = torch.full((len(dates), len(registry.universe.codes)), float("nan"))
    return out
```

稳定要求：

| 要求 | 说明 |
|---|---|
| shape | `[R, N]`，`R` 是日期数，`N` 是股票数。 |
| 缺失值 | 用 `NaN`。 |
| 对齐 | 日期和股票顺序必须对齐 `registry.universe.dates` / `registry.universe.codes`。 |
| 预处理 | rolling、neutralize 等建议写在 `<op>` 或 `ResearchLoader`。 |

## 16. backtest

```xml
<backtest
  output_path="output/backtest"
  daily_metrics_file="daily_pnl.csv"
  cash="10000000.0"
  fee_rate="0.0015"
  reserve_cash="0.95"
  verbose="false"
  universe="base"
  execution_price="vwap30"
  drawdown_stop="0.0"
  cooldown_days="0"
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `output_path` | `output/backtest` | 回测输出目录。 |
| `daily_metrics_file` | `daily_pnl.csv` | 每日 PnL 文件名。 |
| `cash` | `10000000.0` | 初始资金。 |
| `fee_rate` | `0.0015` | 交易费率。 |
| `reserve_cash` | `0.95` | 资金使用/保留比例参数。 |
| `verbose` | `false` | 是否打印更多回测日志。 |
| `universe` | `base` | 回测 universe。 |
| `execution_price` | `vwap30` | 执行价格字段。 |
| `drawdown_stop` | `0.0` | 回撤止损参数。 |
| `cooldown_days` | `0` | 冷却天数。 |

回测持仓生成逻辑由 `<strategy>` 决定；交易和每日指标输出由 `<backtest>` 决定。

## 17. monitor

```xml
<monitor
  enabled="false"
  output_path=""
  format="csv"
  print_summary="true"
  collect_gpu="true"
  sync_cuda="false"
  verbose="false"
/>
```

| 参数 | 默认值 | 说明 |
|---|---|---|
| `enabled` | `false` | 是否启用性能监控。 |
| `output_path` | 空 | 监控 CSV 输出路径；空值时可由输出根目录派生。 |
| `format` | `csv` | 当前稳定格式为 CSV。 |
| `print_summary` | `true` | 结束时是否打印 summary。 |
| `collect_gpu` | `true` | 是否采集 GPU 显存信息。 |
| `sync_cuda` | `false` | 计时时是否同步 CUDA；更准但可能变慢。 |
| `verbose` | `false` | 是否打印主循环进度。 |

## 18. runCombo 输出

运行：

```bash
runCombo config.xml
```

成功后常见产物：

```text
output/
  train.log
  alpha_history.pt
  alpha.parquet
  daily_ic
  backtest/
    daily_pnl.csv
checkpoints/
```

说明：

- `alpha.parquet` 是 `runEval config.xml` 的总体评估核心输入。
- `daily_ic` 是 `runCombo` 同步导出的快速 IC 文件。
- `backtest/daily_pnl.csv` 是回测每日指标。
- 如果 `enable_alpha_analysis="false"`，可能不会生成 `alpha.parquet`，总体评估会失败。

## 19. runEval 评估入口

### 19.1 总体分析

```bash
runEval config.xml
```

或：

```bash
runEval --overall --config config.xml
```

默认检查 `<output_root>/alpha.parquet`，然后基于 alpha 和 config 指向的 label/cache 重新计算：

- IC summary
- PnL summary
- IC/PNL checks
- 10 分组回测
- Barra exposure summary
- top10 excess
- `signal_analysis.png` 长图

默认输出：

```text
<output_root>/eval_report/
  ic_summary.csv
  pnl_summary.csv
  ic_checks.csv
  pnl_checks.csv
  decile_summary.csv
  barra_exposure_summary.csv
  top10_excess.csv
  signal_analysis.png
  report.json
```

常用参数：

```bash
runEval config.xml --start 20200101 --end 20240101
runEval config.xml --skip-deciles
runEval config.xml --skip-exposure
runEval config.xml --report-dir /tmp/eval_report
runEval config.xml --plot-output /tmp/signal_analysis.png
runEval config.xml --label vwap30_label1d.csv --label-is-table --label-5d vwap30_label5d.csv --label-5d-is-table
```

### 19.2 sim: IC 类评估

输入 signal，直接计算 daily IC 并输出 simsummary 风格汇总：

```bash
runEval --sim alpha.parquet --config config.xml
```

显式 label：

```bash
runEval --sim alpha.parquet \
  --label vwap30_label1d.csv --label-is-table \
  --label-5d vwap30_label5d.csv --label-5d-is-table
```

输入已有 daily IC：

```bash
runEval --sim daily_ic --input-is-ic --normalize-names
```

可选落盘：

```bash
runEval --sim alpha.parquet --config config.xml --output daily_ic.csv --summary-output simsummary.txt
```

### 19.3 pnl: PnL 类评估

输入 signal，按 label 转 daily PnL，再输出 pnlsummary 风格汇总：

```bash
runEval --pnl alpha.parquet --config config.xml
```

输入已有 daily PnL：

```bash
runEval --pnl output/backtest/daily_pnl.csv --input-is-pnl
```

常用参数：

```bash
runEval --pnl alpha.parquet --config config.xml --booksize 10000000 --tradecost-ratio 0.5
runEval --pnl output/backtest/daily_pnl.csv --input-is-pnl --pnlzz500 pnlzz500.tsv
```

### 19.4 corr: 两个矩阵相关

用于 position、trade、alpha 等 `date x code` 矩阵：

```bash
runEval --corr pos_a.parquet pos_b.parquet
```

默认只看最近 240 个重叠交易日。

常用参数：

```bash
runEval --corr pos_a.parquet pos_b.parquet --min-valid 1000
runEval --corr pos_a.parquet pos_b.parquet --corr-days 250
```

输出包括平均、最大、最小日频截面相关、有效天数和日期范围。

同时会输出多头持仓重合度：

- 默认取每日共同有效股票池中两边分值最高的 top 10%。
- 日重合度 = 两边 top 集合交集数量 / top 集合数量。
- 摘要字段包括 `avg_long_overlap`、`max_long_overlap`、`min_long_overlap`。

修改 top 比例：

```bash
runEval --corr pos_a.parquet pos_b.parquet --top-pct 5
```

### 19.5 exposure: Barra 暴露

```bash
runEval --exposure alpha.parquet --config config.xml
```

显式 AshareCache：

```bash
runEval --exposure alpha.parquet --ashare-cache-path /path/to/Cache/AshareCache
```

模式：

```bash
runEval --exposure alpha.parquet --config config.xml --exposure-mode 0
runEval --exposure alpha.parquet --config config.xml --exposure-mode 1
```

`0` 表示截面相关暴露，`1` 表示 beta 暴露。输出各 Barra 风格的 `min/mean/max/std`。

### 19.6 va: value-add

输入已有 PnL 文件：

```bash
runEval --va base_daily_pnl.csv new_daily_pnl.csv
```

输入 signal，并用 config 的 label 转 PnL：

```bash
runEval --va base_alpha.parquet new_alpha.parquet --config config.xml
```

常用参数：

```bash
runEval --va base.csv new.csv --column longonly_pnl
runEval --va base.csv new.csv --weights 0.01,0.03,0.05,0.1
runEval --va base.csv new.csv --blended-output blended.csv
```

输出按年度和 full period 展示 base、new 以及不同混合权重对应的年化收益增量。

## 20. 消融和持仓相关

逐特征置零消融：

```bash
comb-ablation-zero config.xml --features 0,1,2
```

常用参数：

```bash
comb-ablation-zero config.xml --feature-group-size 20 --ablation-batch-size 4
comb-ablation-zero config.xml --output-suffix ablation_by_zero
```

两个 position parquet 平均日频相关：

```bash
comb-pos-corr pos_a.parquet pos_b.parquet
comb-pos-corr pos_a.parquet pos_b.parquet --min-valid 1000
```

## 21. 排错与验收

### 21.1 配置无法加载

检查：

- XML 根节点是否为 `<config>`。
- 节点属性是否拼写正确；除 `<model>` 外，未知属性会失败。
- bool 是否写成支持的值。
- dtype 是否是 `float16/float32/float64/bfloat16`。
- 相对路径是否应相对 `config.xml` 所在目录，而不是当前 shell 目录。

### 21.2 数据找不到

检查：

- `constants.cache_path` 是否指向包含 `AshareCache` 的根目录。
- `<item module="builtin.factorsim" path="...">` 的绝对路径是否存在，或相对路径是否能相对当前 XML/data-pack 文件找到。
- label 默认路径是否存在：`AshareCache/1d_DailyLabel/DailyLabel.vwap30_label1d`。
- `strategy.start_ds/end_ds` 是否落在数据日期范围内。

### 21.3 模型接入失败

检查：

- `model_path` 是否存在。
- 文件里是否定义 `ResearchModel`。
- `ResearchModel` 是否实现 `fit/predict/save/load`。
- `predict()` 返回是否是一维 tensor，长度等于可预测股票数。
- `ResearchModel.fit()` 是否匹配 dataset 返回结构。

### 21.4 回测没有正常产出

检查：

- `strategy.start_ds/end_ds` 是否有交易日。
- `enable_alpha_analysis` 是否为 `true`。
- `backtest.output_path` 是否可写。
- 自定义 `AlphaStrategy.generate_positions()` 是否返回 pandas Series。
- 返回权重是否全 NaN、全 0 或没有可交易股票。

### 21.5 runEval 总体评估失败

检查：

- `<output_root>/alpha.parquet` 是否存在且非空。
- 如果 `enable_alpha_analysis="false"`，先改为 `true` 重新跑 `runCombo`。
- config 指向的 label/cache 是否存在。
- 如果 label 是本地表，使用 `--label ... --label-is-table --label-5d ... --label-5d-is-table`。
- 日期过滤后是否还有 alpha 行。

### 21.6 内存或速度问题

建议：

- 降低 `max_train_days`。
- 关闭 `processed_feature_cache`。
- 尝试 `compression="fp4"`。
- 降低模型 `batch_size` 或 `epochs`。
- 调整 `torch_threads`，避免多任务并发时 CPU 线程过量。
- 开启 monitor 定位阶段耗时。

### 21.7 成功验收清单

一次完整训练回测通常应有：

```text
output/train.log
output/alpha.parquet
output/daily_ic
output/backtest/daily_pnl.csv
output/eval_report/report.json
output/eval_report/signal_analysis.png
```

如果启用了 checkpoint，还应有：

```text
checkpoints/
```

一个 agent 接手项目时，优先按这个顺序确认：

1. `config.xml` 可读，路径都能解析。
2. 数据 item 和 label 能找到。
3. `ResearchModel` 接口完整。
4. `runCombo config.xml` 能生成 `alpha.parquet`。
5. `runEval config.xml` 能生成 `eval_report`。
6. 评估结果里 IC、PNL、coverage、exposure 没有明显异常。
